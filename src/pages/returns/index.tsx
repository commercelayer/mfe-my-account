import { NextPage } from "next"

// import Returns from "src/components/composite/Returns"
import Empty from "src/components/composite/Empty"

const ReturnsPage: NextPage = () => {
  // return ( <Returns /> )
  return <Empty type="Returns" />
}

export default ReturnsPage
