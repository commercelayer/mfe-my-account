import styled from "styled-components"
import tw from "twin.macro"

export const Wrapper = styled.div`
  ${tw`w-full flex py-2 justify-center items-center text-center px-2 bg-gray-50 text-gray-500 border border-gray-300 rounded cursor-pointer hover:border-gray-400 transition duration-200 ease-in`}
`

export const Label = styled.p`
  ${tw`text-xs px-0.5`}
`
